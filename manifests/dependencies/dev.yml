#@data/values
---
mysql:
  enabled: true
  helm:
    primary:
      persistence:
        size: 1Gi	

postgres:
  enabled: true  
  helm:
    primary:
      persistence:
        size: 1Gi
    metrics:
      enabled: true

rabbitmq:
  enabled: true  
  helm:
    persistence:
      size: 1Gi
