resources:
  repositories:
    - repository: filipeforattini/ff-iac-azure-pipelines
      type: github
      name: template
      endpoint: filipeforattini

extends:
  template: service.yml@template
  parameters:
    language: node
