resources:
  repositories:
    - repository: filipeforattini/ff-iac-azure-pipelines
      type: github
      name: template
      endpoint: github.com_filipeforattini

extends:
  template: service.yml@template
  parameters:
    language: node
